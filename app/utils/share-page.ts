export const sharePage = () => {
    // TODO: share on socials, show link and/or QR Code, share with mobile context menu if on mobile or via email if on pc
    console.log("Should be able to share page now")
}